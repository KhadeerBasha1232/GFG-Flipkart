{
  "code": "//Initial Template for C++\r\n\r\n#include <bits/stdc++.h>\r\nusing namespace std;\r\n\r\n// } Driver Code Ends\r\n//User function Template for C++\r\n\r\nclass Solution\r\n{\r\nprivate:\r\n    int f(int idx , long long curr , long long sum , vector<int>&arr,vector<vector<long long>>&dp){\r\n        if(idx == arr.size()){\r\n            if(curr == sum) return 1e9;\r\n            return abs(curr-(sum-curr));\r\n        }\r\n        if(dp[idx][curr]!=-1)return dp[idx][curr];\r\n        int take = f(idx+1,curr+arr[idx],sum,arr,dp);\r\n        int notTake = f(idx+1,curr,sum,arr,dp);\r\n        return dp[idx][curr] = min(take,notTake);\r\n    }\r\npublic:\r\n    int process_distribution(int N, vector<int> &A)\r\n    {\r\n        long long sum = accumulate(A.begin(),A.end(),0ll);\r\n        vector<vector<long long>>dp(N+1,vector<long long>(sum+1,-1));\r\n        return f(0,0,sum,A,dp);\r\n    }\r\n};\r\n\r\n//{ Driver Code Starts.\r\nint main()\r\n{\r\n    int t;\r\n    cin >> t;\r\n    while (t--) {\r\n        int N;\r\n        cin >> N;\r\n        vector<int>A(N);\r\n        for (int i = 0; i < N; ++i)\r\n        {\r\n            cin >> A[i];\r\n        }\r\n        Solution ob;\r\n        int ans = ob.process_distribution(N, A);\r\n        cout << ans << \"\\n\";\r\n    }\r\n    return 0;\r\n}\r\n\r\n// } Driver Code Ends",
  "status": "1",
  "language": "cpp",
  "user_code": "//User function Template for C++\r\n\r\nclass Solution\r\n{\r\nprivate:\r\n    int f(int idx , long long curr , long long sum , vector<int>&arr,vector<vector<long long>>&dp){\r\n        if(idx == arr.size()){\r\n            if(curr == sum) return 1e9;\r\n            return abs(curr-(sum-curr));\r\n        }\r\n        if(dp[idx][curr]!=-1)return dp[idx][curr];\r\n        int take = f(idx+1,curr+arr[idx],sum,arr,dp);\r\n        int notTake = f(idx+1,curr,sum,arr,dp);\r\n        return dp[idx][curr] = min(take,notTake);\r\n    }\r\npublic:\r\n    int process_distribution(int N, vector<int> &A)\r\n    {\r\n        long long sum = accumulate(A.begin(),A.end(),0ll);\r\n        vector<vector<long long>>dp(N+1,vector<long long>(sum+1,-1));\r\n        return f(0,0,sum,A,dp);\r\n    }\r\n};\r\n"
}